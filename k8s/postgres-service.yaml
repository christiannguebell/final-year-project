apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: seas
  labels:
    app: postgres
spec:
  type: ClusterIP  # Internal only — only pods inside the cluster can reach the database
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
